# Testes do runtime NVS

- `integration.php`: prova PHP/MariaDB de autenticação, isolamento, cifra, deduplicação, retry e contratos capturados dos destinos.
- `run-compose-integration.sh`: sobe e remove um projeto Compose isolado para executar a prova.
